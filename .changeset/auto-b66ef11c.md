---
"web": patch
---

design-studio: fix the AI provider/model dropdowns overflowing off-screen, make Generate with AI aware of elements already on the canvas (not just its own prior draft), dim the real canvas while a draft preview is showing, and swap the draft outline from red to the app's teal accent
