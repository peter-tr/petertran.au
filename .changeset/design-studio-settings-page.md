---
"web": minor
---

Add a dedicated `/design-studio/settings` page, linked from the gallery header, showing the AI provider/model tier controls (previously only editable inline in the editor's AI panel) alongside a new "Portfolio data access" toggle for the new `allowSupergraphQuery` setting.
