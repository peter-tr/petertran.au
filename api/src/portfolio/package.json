{
  "name": "portfolio",
  "private": true,
  "version": "1.2.0",
  "type": "module",
  "scripts": {
    "build": "esbuild handler.ts --bundle --platform=node --target=node20 --format=esm --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\" --outfile=dist/handler.mjs --external:@aws-sdk/* && cp schema.graphql dist/schema.graphql",
    "test": "vitest run"
  },
  "dependencies": {
    "api-shared": "1.1.0",
    "@apollo/server": "^4.11.0",
    "@as-integrations/aws-lambda": "^3.1.0",
    "@aws-sdk/client-cloudwatch": "^3.1080.0",
    "@aws-sdk/client-cost-explorer": "^3.1080.0",
    "@aws-sdk/client-sesv2": "^3.1080.0",
    "@aws-sdk/client-xray": "^3.1080.0",
    "graphql": "^16.9.0"
  }
}
