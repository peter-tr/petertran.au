{
  "name": "supergraph",
  "private": true,
  "version": "0.1.2",
  "type": "module",
  "scripts": {
    "codegen": "tsx scripts/compose-supergraph.ts",
    "build": "esbuild handler.ts --bundle --minify --platform=node --target=node20 --format=esm --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\" --outfile=dist/handler.mjs --external:@aws-sdk/*",
    "test": "vitest run"
  },
  "dependencies": {
    "api-shared": "*",
    "@apollo/gateway": "^2.9.0",
    "@apollo/server": "^4.11.0",
    "@as-integrations/aws-lambda": "^3.1.0",
    "graphql": "^16.9.0"
  },
  "devDependencies": {
    "@apollo/composition": "^2.9.0"
  }
}
