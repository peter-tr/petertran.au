// Trimmed to the units actually in use - pantry-api's normalizeUnit alias
// table (pantry-api/src/lib/normalize.ts) still recognizes more than this,
// so anything added directly via the API still normalizes fine.
export const UNIT_OPTIONS = ["pcs", "g", "kg", "mg", "L", "mL"];
